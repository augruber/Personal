import React from "react";

const RollingSphereCar: React.FC = () => (
  <article className="grid gap-8">
    <h1 className="text-2xl md:text-3xl font-semibold tracking-tight">Rolling Sphere Car</h1>

    {/* Tagline */}
    <p className="text-lg italic">
      A car, confined to a LED sphere, which when driven causes the sphere to roll controllably across flat surfaces. 
    </p>

  </article>
);

export default RollingSphereCar;